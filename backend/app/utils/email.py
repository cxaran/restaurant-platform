
async def send_email(
        subject: str, 
        email_to: str, 
        template_name: str,
        template_context: dict[str, str]) -> None:
    # Implementación de envío de correo electrónico utilizando un servicio externo
    pass